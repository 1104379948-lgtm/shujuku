# SP·数据库 test29 卡顿问题反馈

> 反馈对象：开发者（AlbusKen/shujuku）
> 测试版本：test29.js（对照 8.8.1.js）
> 日期：2026-08-08

## 1. 现象概述

SQLite 模式下，长聊天存在明显卡顿：进入聊天、编辑表格保存、重新生成（含删除旧表）时主线程长时间冻结。三个场景的日志指向同一个 `v2-replay` 性能埋点：test29 单次全量回放约 10.5~11.5 秒，且同一个操作会多次触发回放。**8.8.1 同样存在该问题**——单次回放约 2.85 秒，一次编辑保存共 4 次回放约 11.4 秒，本身已可感知；test29 将其进一步放大。

## 2. 环境与数据

- 聊天规模：125 楼，AI 楼层 63，带帧消息 62 条，17 张表
- 帧 / 操作统计：62 帧，70 条日志条目，**660 条 operation（sql_sheet_batch 652 / data_replace 3 / row_upsert 5）**
- 整聊天 JSON 约 2.3MB，`JSON.stringify` 仅 11.8ms——聊天保存本身不是瓶颈
- 存储模式：SQLite

## 3. 发现过程

### 3.1 日志证据

脚本自带 `[Performance]` 埋点，三个场景均反复出现同一类日志：

```
v2-replay durationMs=10665.1 / 11543.5 / 10699.4 / 10636.5 / 10540.7 / 10543.9
metrics={"messageCount":125~126,"maxMessageIndex":-1|124,"success":true,"baseKind":"full_checkpoint","sheetCount":17}
```

实测汇总：

| 场景 | 8.8.1 | test29 |
| --- | --- | --- |
| 进入聊天 | 2 次回放，每次 ~2.85s（共 ~6s） | 2 次回放，每次 ~10.6s（共 ~21s） |
| 编辑表格保存一次 | 4 次回放，每次 ~2.85s（共 ~11.4s） | 5 次回放，每次 ~10.5~11.5s（共 ~54s） |
| 重新生成（含删除旧表） | — | 至少 1 次回放 ~10.7s |

### 3.2 调用链（test29 一次编辑保存）

1. `applyVisualizerPendingDataOps_ACU`（数据操作前校验回放）→ 11.5s
2. `persistTableMutationLogBatchV2Core_ACU`（持久化内回放，maxMessageIndex=124）→ 10.7s
3. `refreshVisualizerRuntimeFromReplay_ACU`（保存后编辑器刷新回放）→ 10.6s
4. `reloadStorageProvider` → `mergeAllIndependentTables_ACU` → `loadTableStateFromFramesV2_ACU`（provider 重载回放）→ 10.5s
5. 同 4 → 10.5s

进入聊天为同一函数两次全量回放（maxMessageIndex=-1），各 ~10.6s；重新生成 / 删表同样经 `commitCurrentFloorTemplateChanges` 触发该回放。

## 4. 根因分析

### 4.1 每次打开 / 保存 / 提交都做全历史回放

`loadTableStateFromFramesV2Detailed_ACU` 从最早的 full checkpoint 一直重放到目标楼层。本聊天 full checkpoint 靠前、目标为最新楼层，因此**每次回放都重放全部 62 帧、660 条 operation**；`maxMessageIndex` 为 -1 还是 124 不影响成本（124 同样覆盖全部历史）。

触发点：

- 打开聊天：启动路径 2 次全量回放
- 编辑保存：一次保存 4~5 次回放（校验、persist、编辑器刷新、provider reload）
- 发送后填表 / 重新生成：`loadV2ReplayMergeBase_ACU`、`commitCurrentFloorTemplateChanges`（含硬删除校验）、persist 各自回放

因此成本随聊天增长呈**平方级**：每条新消息既新增 operation，又使之后每次回放更慢。

### 4.2 单次回放慢：两个版本共有的结构问题

8.8.1 的单次回放 ~2.85s 并非正常水平，它同样存在以下可优化的结构问题（test29 全部保留）：

1. **逐 SQL 操作重建表别名映射**：对每条 `sql_sheet_batch`（本聊天 652 条），`applySqlBatchOperationV2_ACU` 调用 `buildReplaySqlTableAliases_ACU` → `buildSheetTableAliasMap_ACU([state], { includeExtendedAliases: true })`，每次解析全部 17 张表的 DDL / 物理名 / canonical 名称，再逐条 token 级重写语句；
2. **帧级 checkpoint / 非 SQL 提交时重建整个 SQLite 引擎**：`commitReplayCandidate_ACU` → `reloadSqlReplayRuntime_ACU` 新建 SqliteEngine 并把整份表数据重新灌入（伴随整份 state 深拷贝 + `normalizeReplayState_ACU` 全表遍历）；本聊天 62 帧多数带 checkpoint，累计触发多次；
3. **每次打开 / 保存 / 重新生成都全历史重放**（见 4.1）。

test29 在 8.8.1 的基础上又增加了**逐 SQL 操作重建列别名注册表**：`buildSheetColumnAliasMap_ACU(state, { supplementalSources: [模板快照] })` 对每张表解析 DDL、整行表头跑拼音映射 `mapSqlColumnIdentifiers_ACU`、逐列 canonical 化、遍历 supplemental 模板源、维护冲突 / 证据映射，再叠加 `rebindSqlMutationColumnReferences_ACU` 列级重写。8.8.1 只有轻量版列别名，无列重绑。652 条 SQL 操作 × 全表列映射构建 ≈ 10s，是 test29 相对 8.8.1 的主要增量。

### 4.3 版本对比（8.8.1 vs test29，均为本聊天实测）

| 版本 | SQL 操作回放做的事 | 单次回放耗时 |
| --- | --- | --- |
| 8.8.1 | 语句规范化 + 逐操作重建表别名 + token 级表名重写 + SQLite 执行 | ~2.85s |
| test29 | 8.8.1 全部 + 逐操作重建列别名（拼音映射、模板 supplemental 全量遍历）+ token 级列名重写 | ~10.7s（约 3.7 倍） |

结论：**两个版本都属于同一结构性问题**——逐操作重建别名映射、帧级 SQLite 引擎整库重建、全历史重放。8.8.1 单次 ~2.85s、一次编辑保存 ~11.4s 已经偏慢且可优化；test29 新增列重绑后单次放大到 ~10.7s，再叠加一次操作内多次回放，造成十秒级主线程冻结。这不是 test29 独有回归，而是同一架构问题在新版本中的加剧。

## 5. 复现方法

1. 保留 console 的 `[Performance]` 输出（脚本自带埋点）
2. 在 SQLite 模式、长聊天（本报告约 125 楼 / 660 条 operation）中执行：进入聊天、编辑表格保存、重新生成 / 发送消息
3. 观察 `v2-replay durationMs=...` 日志的数量与耗时
